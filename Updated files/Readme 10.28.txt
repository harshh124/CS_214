This contains the updated code of arg.c 

compile in ilab using 
gcc -o arg arg.c 

run it with any
./arg -c fieldname -d dir -o dir

./arg -c fieldname 

./arg -c fieldname -o dir 

./arg -c fieldname -d dir


if new files arent being created then check if the function call to file_sort isnt commented out in the arg.c file. 

Harsh

 

